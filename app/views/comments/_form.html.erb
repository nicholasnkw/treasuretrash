<%= form_for([@posting, @comment]) do |f| %>
  <div class="form-group">
    <%= f.text_area :comment, class: "form-control" %>
  </div>
    <%= f.submit "Comment", class: "btn btn-default" %>
<% end %>
