<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="/">TreasureTrash</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav">
        <li class=""><%= link_to "Post Item", new_posting_path %></li>
      </ul>
      <form class="navbar-form navbar-left" action= "/search" method= "get", >
        <div class="input-group" style="width: 750px">
          <input type="text" class="form-control" name="any">
          <span class="input-group-btn">
            <button type="submit" class="btn btn-default">Search</button>
          </span>
        </div>
        
      </form>
    <% if user_signed_in? %>    
      <ul class="nav navbar-nav navbar-right" style="margin-right: 0px">
	      <li class=""> <%= link_to ("Credits: " + current_user.credit.to_s), user_path(current_user) %></li>
        <%= image_tag (current_user.avatar.nav_avatar.url.to_s), class: "img-circle pull-left"%>

        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown" role="button" aria-haspopup="true" aria-expanded="false"><%=current_user.first_name%> <span class="caret"></span></a>
          <ul class="dropdown-menu">
            <li><%=link_to "Profile", user_path(current_user)%></li>
            <li><%= link_to "Transactions", transactions_user_path(current_user)%></li>
            <li><%= link_to "Postings", postings_user_path(current_user)%></li>
            <li role="separator" class="divider"></li>
            <li><%= link_to "Logout", destroy_user_session_path, method: :delete%></li>
          </ul>
        </li>
       <%else %>
	      <ul class="nav navbar-nav navbar-right">
	          <li><%= link_to "Login", new_user_session_path unless user_signed_in? %></li>
	          <li><%= link_to "Sign in with Facebook", user_facebook_omniauth_authorize_path unless user_signed_in? %></li>
          </ul>
        <%end%>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>
